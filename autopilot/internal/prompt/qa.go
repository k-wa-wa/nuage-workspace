package prompt

import "fmt"

// qaPrinciples はQAフェーズにおける効率的な行動原則である。
// 旧 nuage-agent (src/agents/qa/agent.ts) の QA_PRINCIPLES をそのまま移植したもの。
const qaPrinciples = `## 効率的な行動原則（重要）
- **重複調査の禁止**: 「gh pr view」 などの確認・調査コマンドを何度も繰り返し実行しないこと。
- **無駄な試行の抑制**: 検証コマンド（テスト、脆弱性スキャンなど）やラベル変更・マージ処理が失敗した場合は、エラーログを注意深く読み、原因に応じた適切な対処を行うこと。
- **セッション制限の意識**: APIセッション制限を回避するため、不要なコマンド実行やファイル探索を最小限に留め、速やかに検証を完了させること。`

// qaVerificationItems はQAフェーズの検証項目である。%[1]d は対象 PR 番号を指す。
// 旧 nuage-agent の QAAgent#getVerificationItems 相当。
const qaVerificationItems = `## 検証項目
1. **最新状態の統合確認**:
   作業ブランチにマージ先（mainやmaster）の最新コミットを取り込み、競合がなく正常にビルドできるかを確認する。
2. **結合・E2Eテスト**:
   統合テストやE2Eテスト、システム全体に関わる動作確認を実行する（例: 「npm run test:integration」など）。
3. **セキュリティ監査**:
   脆弱性スキャン（例: 「npm audit」など）を実行し、セキュリティ上の問題を持つ依存関係が新規追加されていないか確認する。
4. **完了基準チェックリストの確認と自己修復（最重要）**:
   PRの概要欄（Body）および紐づく親Issueの概要欄を 'gh pr view %[1]d' や 'gh issue view <Issue番号>' で取得し、記載されているすべての完了基準（チェックリストの '- [ ]' 項目）にチェック（'- [x]'）が入っていることを確認する。
   **【自己修復ルール】** テストやビルドは正常に合格しているにもかかわらず、単に未チェックの項目（'- [ ]'）が残っている場合は、開発フェーズに差し戻すのではなく、QAエージェント自身がすべての完了基準を '- [x]' に更新（自己修復）した上で、そのままマージ（合格）処理へ進むこと。
   更新の際、長文によるシェルエスケープのエラーを防ぐため、必ず一時ファイルを用いてPRおよびIssueの概要欄を更新し、直後に削除すること。
   コマンド例: 'echo "[すべての完了基準を- [x]にした本文]" > pr_body.md && gh pr edit %[1]d --body-file pr_body.md && rm pr_body.md'
   （※テストが失敗しているか、実装そのものが不十分で未チェックになっていると判断される場合は検証不合格（Failed）とする）`

// BuildQA は qa worker 向けのプロンプトを組み立てる。
// マージ直前のシステム全体としての品質・安全性の最終検証を行わせる。
//
// 旧 nuage-agent の QAAgent#buildPrompt 相当。旧実装は autoMerge フラグにより
// 「検証合格後に自動マージまで行う」分岐を持っていたが、DESIGN.md に自動マージに
// 関する記述がなく、PR のマージ可否は人間が判断すべき事項であるため、本移植では
// 常に「検証サマリを投稿し、ユーザーにマージを委ねる」手動マージの分岐のみを
// 採用している。
//
// DESIGN.md 8章への移行に伴い、旧実装が持っていたラベル遷移
// （合格時の agent:qa 剥がし、不合格時の agent:dev 付与・agent:qa 剥がし）の
// 指示は取り除いた。合否はコメントで表明するのみとし、次に何をするか
// （dev worker に差し戻すかどうか）は dispatcher がコメント内容から判断する。
func BuildQA(ctx Context) string {
	return fmt.Sprintf(`あなたは対象リポジトリ「%[1]s」のQAエージェント (QAAgent) である。
GitHub Pull Request #%[2]d の最終検証を行い、以下の手順を実行すること。

単体テスト等の個別検証はすでに開発（Dev）段階で完了している。ここでは、マージ直前のシステム全体としての品質と安全性を検証すること。

---

%[3]s

---

%[4]s

## アクション手順

1. **ブランチのチェックアウト**
   対象のPRブランチをローカルにチェックアウトする。
   コマンド: 「gh pr checkout %[2]d」

2. **検証の実行**
   上記の「検証項目」を実行する。

3. **検証結果の処理**
   - **検証に合格した場合（Passed）**:
     実行したテスト内容、ビルド結果、監査レポートなどの「検証サマリ」を作成し、PRコメントとして投稿してユーザーにマージを委ねる。
     コマンド:
       - サマリコメント投稿: 「gh pr comment %[2]d --body "[QA検証サマリ...]

検証に合格したため、手動でのマージを求める。"」
   - **検証に合格しなかった場合（Failed）**:
     失敗したテスト、競合内容、検出された脆弱性、または完了基準チェックリストに未チェック項目（'- [ ]'）が残っていること（※自己修復が不可能な実装不備の場合）などの不合格理由をPRコメントとして投稿する。
     コマンド:
       - 不合格コメント投稿: 「gh pr comment %[2]d --body "[検証失敗の詳細、または未完了の完了基準（- [ ] の項目）とその原因]"」

---

%[5]s
`, ctx.RepoName, ctx.Number, qaPrinciples, fmt.Sprintf(qaVerificationItems, ctx.Number), awaitingUserReviewNote)
}
